# 배포용 최소 의존성 목록.
# requirements.txt(전체 개발용)에는 데이터 전처리/모델 학습에만 쓰이는 무거운
# 패키지(torch, pyreadr, pyarrow, pypdf, pytest 등)가 섞여있는데, 그건 모델/벡터DB를
# "만드는" 단계에서만 필요하고 이미 만들어진 결과물(models/, vectorstore/)을
# "서빙"만 하는 배포 서버엔 필요 없다. Docker 이미지 용량/빌드 시간을 줄이려고 분리함.

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0

sqlalchemy>=2.0.0
mysql-connector-python>=8.3.0

joblib>=1.4.0
numpy>=1.24.0
scikit-learn>=1.3.0

chromadb>=0.4.22
langchain>=0.1.0
langchain-community>=0.0.20

openai>=1.14.0  # Groq API 호출용 (OpenAI SDK 호환)
python-dotenv>=1.0.0
