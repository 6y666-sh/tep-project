# ===== 데이터 처리 =====
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
pyarrow>=15.0.0   # parquet 저장/로드용 (data_pipeline/convert_to_parquet.py)
pyreadr>=0.5.0    # TEP 원본 .RData 파일 로드용

# ===== 이상탐지 (베이스라인) =====
# pyod는 Python 3.14 환경에서 의존성(numba/llvmlite) 소스 빌드 실패로 제외했었음.
# pyod가 내부적으로 감싸는 scikit-learn의 IsolationForest를 직접 사용 중 (위 scikit-learn으로 충분)
# pyod>=1.1.0

# ===== 시계열 고도화 (선택, LSTM 등) =====
torch>=2.0.0

# ===== RAG: 임베딩 + 벡터DB =====
# sentence-transformers는 torch가 필수 의존성이라 3.14에서 설치 불가라 뺌.
# chromadb 자체에 내장된 기본 임베딩 함수(onnxruntime 기반, MiniLM-L6)를 사용.
# langchain은 torch 의존성이 없어서 별도 설치 시 문제 없음 (텍스트 분할/체이닝용)
chromadb>=0.4.22
langchain>=0.1.0
langchain-community>=0.0.20

# ===== LLM API 연동 (파인튜닝 없이 API 호출만 사용) =====
anthropic>=0.25.0
openai>=1.14.0  # 선택: 임베딩/LLM 대체 옵션

# ===== API 서버 =====
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0

# ===== DB (5단계 FastAPI 통합 전까지는 불필요, 지금은 주석 처리) =====
# cryptography가 Rust(cargo) 소스 빌드를 요구해서 설치 실패하는 이슈가 있었음.
# DB 붙이는 시점(5단계)에 다시 풀고 원인 해결할 것.
# sqlalchemy>=2.0.0
# pymysql>=1.1.0
# cryptography>=42.0.0  # pymysql 인증 플러그인용

# ===== 문서 전처리 (KOSHA GUIDE PDF/HWP 파싱) =====
pypdf>=4.0.0
python-dotenv>=1.0.0

# ===== 테스트 =====
pytest>=8.0.0
httpx>=0.27.0  # FastAPI TestClient용
